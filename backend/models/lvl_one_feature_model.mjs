export default function LvlOneFeature(lvl_one_id, timeline, connection_type, user_id) {
    this.lvl_one_id = lvl_one_id;
    this.timeline = timeline;
    this.connection_type = connection_type;
    this.user_id = user_id;
}