export default function LvlOneFeature(lvl_one_id, timeline, connection_type, content_order) {
    this.lvl_one_id = lvl_one_id;
    this.timeline = timeline;
    this.connection_type = connection_type;
    this.content_order = content_order;
}