export default function Trait(trait_id, user_id, posting_trait, commenting_trait, reacting_trait, messaging_trait, updating_trait, comm_trait, notification_trait){
    this.trait_id = trait_id;
    this.user_id = user_id;
    this.posting_trait = posting_trait;
    this.commenting_trait = commenting_trait;
    this.reacting_trait = reacting_trait;
    this.messaging_trait = messaging_trait;
    this.updating_trait = updating_trait;
    this.comm_trait = comm_trait;
    this.notification_trait = notification_trait;
}