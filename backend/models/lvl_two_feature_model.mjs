export default function LvlTwoFeature(lvl_two_id, commenting, account_type, identity, messaging_mem, messaging_control, messaging_audience, sharing, reactions) {
    this.lvl_two_id = lvl_two_id;
    this.commenting = commenting;
    this.account_type = account_type;
    this.identity = identity;
    this.messaging_mem = messaging_mem;
    this.messaging_control = messaging_control;
    this.messaging_audience = messaging_audience;
    this.sharing = sharing;
    this.reactions = reactions;
}